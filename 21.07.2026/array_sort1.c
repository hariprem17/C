//sort ascending

#include<stdio.h>

void main()
{
    int a[]={40,20,70,60};
    int n=sizeof(a)/sizeof(a[0]);

    int min=a[0];
    for(int i=1;i<n;i++)
    {
       if(a[i]<a[0])
    }
    for(int i=0;i<n;i++)
    {
       printf("%d",a[i]);
    }



}

